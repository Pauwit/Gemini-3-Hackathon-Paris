# Drive Search Skills

## Commands
- `gws drive search --query "<query>" --limit <n>` — search files
- `gws drive get --id <fileId>` — get file content

## Output
{ "files": [{ "id": string, "name": string, "mimeType": string, "content": string, "modifiedTime": string }] }
